# ESLint config
# http://eslint.org/docs/user-guide/configuring
# https://github.com/BigstickCarpet/eslint-config-modular

extends:
  - modular/es6
  - modular/node

rules:
  # Disable this rule so we can use "new Postman()" in tests
  no-new: off
