extends:
  - modular/test

globals:
  postmanBDD: true
  tests: true

rules:
  # Require "use strict" pragmas inside function closures
  # so that test specs can be safely used in a web browser
  strict:
    - error
    - function
